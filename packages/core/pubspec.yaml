name: core
description: Core infrastructure, routing, DI, and utils.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  go_router: ^16.0.0
  maac_mvvm_with_get_it: ^0.2.1+2
  maac_mvvm_annotation: ^0.0.2
  intl: ^0.20.2
  path: ^1.9.1
  path_provider: ^2.1.0
  xml: ^6.5.0
  archive: ^3.3.6
  localization:
    path: ../localization

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
