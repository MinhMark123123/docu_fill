import 'package:flutter/material.dart';

class MainLayoutMobile extends StatelessWidget {
  final Widget child;
  const MainLayoutMobile({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
